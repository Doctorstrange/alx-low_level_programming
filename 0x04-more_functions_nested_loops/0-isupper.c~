#include "main.h"

/**
 * _isupper - checks if the letter is in upper case
 *@x: number to be checked 
 *return : 1 for upper and 0 for lower
 */

int _isupper(int x);
{
if (x >= 65 && x <= 90)
{
return (1);
}
return (0);
}
